@import "../../style/globals"
@import "./connect"

keyboardColor = #9299A3;

body
	background keyboardColor; overflow hidden;

#appContainer, #dogoApp
	height 100%;

html, button, input, select, textarea
	line-height 120%; font-family "Open Sans"; font-size 16px;

#viewport
	background keyboardColor; font-family "Open Sans"; font-size 16px;
	color #223;
	// background -webkit-radial-gradient(center, circle cover, #FFE2AA 0%, #FF9F36 120%);
	-webkit-touch-callout: none;
	-webkit-user-select: none; /* Disable selection/Copy of UIWebView */
	-webkit-transition -webkit-transform .3s;
	
	.appBackground
		overflow-y scroll; -webkit-overflow-scrolling touch; absolute top 0 left 0; background #222;
		.link
			color #126EAF;
	
	.appForeground
		// background -webkit-radial-gradient(center, circle cover, #227EBF 0%, lighten(#227EBF, 80%) 200px);
		// #227EBF, #025E9F
		// gradient(#0073FF, lighten(#0073FF, 50%))
		// gradient(#0073FF, lighten(#0073FF, 75%))
		// gradient(lighten(#0073FF, 10%), lighten(#0073FF, 75%))
		// gradient(appBackgroundFrom, appBackgroundTo)
		background #f0f0f0;
		// gradient(#00A3FF, lighten(#30A4FF, 75%))
		// gradient(#0070FF, lighten(#30A4FF, 75%))
		// gradient(lighten(#227EBF, 25%), lighten(#025E9F, 75%))
		height 100%;
		.foregroundOverlay
			box-shadow -2px 0 2px rgba(0,0,0,.75);

.tags-overlay .tags-overlay-content
	text-align center; font-size 18px; color #fff; text-shadow 0 1px 1px rgba(0,0,0,.9);

.head
	height 0;
	.corner
		overflow hidden;
		gradient(rgba(buttonFrom, .95), rgba(buttonTo, .95));
		// toolBorder()
		// box-shadow 0 1px 2px rgba(0, 0, 0, .7);
	.corner.active
		background rgba(cornerActive, .97)
	.corner.left
		absolute top 0 left 0;
		.logoIcon, .back
			absolute top 0 right -2px;
	.corner.right
		.search
			absolute top 0 left -2px;
	
	.title
		padding 0 4px; color #fff;
		font-size 21px; text-align center; padding 7px 0 0 0;
		max-width 225px; text-overflow ellipsis; white-space nowrap; overflow hidden; margin 0 auto;
		.logo
			margin-top 4px;
	.search-control
		.input
			inputWidth = 300px;
			width inputWidth; height 30px;
			absolute top 4px right; background #fff;
			border 2px solid color3; border-radius 3px;
			-webkit-transform translateX(inputWidth + 30px)
			-webkit-transition -webkit-transform .4s;
	.search-control.expand
		.input
			-webkit-transform translateX(3px)
	.devBar
		display none;
		absolute top right 50px;
		.button
			margin 4px 2px 0;
	
.homeView
	padding-top 20px;
	.logoName .icon
		display block; margin 0 auto;
	.conversations
		min-height 254px; padding 4px 0 5px 0;

.searchUI .resultsBox
	absolute top 0 left 0; width 100%; box-shadow 0px -1px 1px rgba(0,0,0,.9);
	gradient(lighten(#227EBF, 75%), lighten(#227EBF, 25%))
	overflow-y scroll; -webkit-overflow-scrolling touch; overflow-x hidden;
	.result
		gradient(#eee, #ccc); float left; overflow hidden;
		.names
			margin -22px 0 10px 4px; color #fff; text-shadow 0 -1px 1px rgba(0,0,0,.5); max-width 100px; overflow hidden;

#composerCanvas
	gradient(#9199A4, #444E5C);

#customKeyboard
	gradient(keyboardFrom, keyboardTo);
	.content
		padding-top 5px;

.glassBar
	border-top 1px solid #025E9F;
	twoTone(
		rgba(appBackgroundFrom, .5), rgba(darken(appBackgroundFrom, 20%), .5),
		rgba(darken(appBackgroundFrom, 40%), .5), rgba(darken(appBackgroundFrom, 60%), .5),
	);
