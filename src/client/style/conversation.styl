@import "./macros"

faceDim = 25px
.face
	width faceDim; height faceDim; background-size faceDim faceDim;
	.member-ribbon
		width 16px; height 16px; background url("/blowtorch/img/badge.png"); float right; margin 32px 0 0;

.conversation
	.messages
		padding 0 0 44px 0;
	.messageBubble
		.textContent
			padding 4px 6px; light();
		.picture, .pictureContent
			border-radius 2px;
			
	.dogo-info
		width 100%; margin 8px 0; padding 4px 0 10px 0; border-width 1px 0; border-style solid;
		.encouragement
			font-size 23px; margin 0 0 4px 0;
		.personal
			font-size 18px; margin 0 0 3px 0;
		.button
			margin-top 4px;
	
	.composer
		position absolute; bottom 0; width 320px; padding 0;
		.surface
			bodyRadius = 6px; bodyWidth = 320px;
			.writer
				display inline-block; width bodyWidth; box-shadow 0 -1px 6px #444;
				textarea
					border none; width bodyWidth - 4px; height 50px;

		.tools
			.right
				float right;
			.tool
				display inline-block; margin 2px 0 1px 4px; border-radius 4px; height 18px; width 24px; padding 4px 8px 6px;
				.icon.draw
					icon(24px, 20px, 'white/98-palette.png')
				.icon.write
					icon(24px, 22px, 'white/09-chat-2.png')
				.icon.photo
					icon(24px, 18px, 'white/86-camera.png')
			.tool.send
				float right; margin-right 1px;


.messageBubble
	margin 8px 8px 0 36px; min-height 50px; border-radius 3px; word-wrap break-word;
	background url('/blowtorch/img/paper/exclusive_paper-trans.png')
	.face
		float left; border-radius 18px; margin 1px 0 0 -31px; width 25px; height 25px; border 1px solid #fff;
.messageBubble.fromMe
	margin 8px 36px 0 8px;
	.face
		float right; margin 1px -31px 0 0;

.draw-composer
	line-height 0; z-index 1;
	position absolute; bottom 0;
	.controls-pos
		position absolute; bottom 0; z-index 1;
	.controls
		line-height 20px; padding 2px 0; box-shadow 0 -2px 3px #222; width 100%; height 33px;
		background url('/blowtorch/img/background/orange-linen.png'); background-size 654px 654px;
		.picker
			position absolute;
		.colorPicker
			bottom 45px; left 5px;
		.penPicker
			bottom 45px; left 55px; box-shadow 0 0 1px #ccc;
		.right
			float right; position relative; bottom 1px;
			.button
				margin 0 2px; padding 4px 10px 6px;
	.close.button
		position absolute; z-index 1; font-size 14px; padding 11px 7px;
